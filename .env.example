# ===========================================================================
# MAS AI WORKFLOW CONFIGURATION
# ===========================================================================

# WORKFLOW_STRATEGY sets the operational mode:
# - "predefined": Follows scenario.xlsx steps & traces Figma paths. (Requires Figma token)
# - "autonomous": Uses LLM to plan its own steps toward the goal. (No Figma required)
WORKFLOW_STRATEGY=predefined

# ===========================================================================
# CORE ADAPTERS
# ===========================================================================

# Android Device ID (get via 'adb devices')
TARGET_DEVICE=your_device_id_here

# Figma Access (Required for Predefined mode)
FIGMA_ACCESS_TOKEN=your_figma_access_token_here
FIGMA_MCP_URL=https://mcp.figma.com/mcp

# ===========================================================================
# AGENT PROVIDERS & MODELS
# ===========================================================================
# providers: openrouter | blackbox | openai | local

# Observer (Vision model - Analyzes screenshots)
OBSERVER_PROVIDER=blackbox
OBSERVER_API_KEY=your_api_key_here
OBSERVER_MODEL=gemini-1.5-flash

# Decider (Logic/Strategic model - Decides UI interactions)
DECIDER_PROVIDER=blackbox
DECIDER_API_KEY=your_api_key_here
DECIDER_MODEL=blackboxai/minimax/minimax-m2.5

# Reflector (Verification model - Evaluates if action was successful)
REFLECTOR_PROVIDER=blackbox
REFLECTOR_API_KEY=your_api_key_here
REFLECTOR_MODEL=gemini-1.5-flash

# Orchestrator (Planning model - Used for Autonomous mode & Figma discovery)
ORCHESTRATOR_PROVIDER=blackbox
ORCHESTRATOR_API_KEY=your_api_key_here
ORCHESTRATOR_MODEL=gemini-1.5-flash

# ===========================================================================
# SYSTEM SETTINGS
# ===========================================================================
LOCAL_LLM_URL=http://localhost:11434/v1
MAX_TOKENS=15000
OUTPUT_DIR=outputs
TOKEN_CONTEXT_WINDOW=128000
TOKEN_WARN_THRESHOLD=0.75
